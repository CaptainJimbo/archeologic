# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.python-version

# Node / frontend
node_modules/
web/dist/
dist/

# Fetched corpus & large artifacts (wiki notes ARE committed; raw sources are not)
corpus/
data/raw/
*.pdf

# Agent run outputs — regenerable investigation results (fetched docs + notes).
# Promote a curated run into wiki/ by hand if it's worth keeping.
runs/

# Env / secrets
.env
.env.*
*.pem

# OS / editor
.DS_Store
.vscode/
.idea/

# Logs / caches / scratch
*.log
.cache/
.playwright-mcp/
.ipynb_checkpoints/
